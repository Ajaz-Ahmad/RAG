# Core dependencies
requests
beautifulsoup4
openai
numpy
faiss-cpu
rank_bm25
sentence-transformers

# Config
pydantic-settings

# Dev (not installed in Docker — use pip install pytest separately for local testing)
# pytest
